name: unit tests
on: [pull_request]
jobs:
  go-test:
    name: runner / go-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.19'
      - name: test
        run: go test -v ./...
